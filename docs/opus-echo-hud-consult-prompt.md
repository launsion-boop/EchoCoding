你是一个资深语音交互与实时 ASR 架构专家。请在当前仓库直接读代码，做根因诊断 + 可落地修复方案。

# 项目背景
- 仓库: /Users/leilangsheng/EchoCoding
- 分支: codex/noise-control-profile-ui
- 目标场景: macOS 上的 ASK HUD（TTS + streaming ASR + VAD）支持低延迟、多轮、近实时对话
- 当前最痛问题:
  1) 串音仍在：TTS 文本被 ASR 识别进去（提示词回读）
  2) 用户体感“HUD弹出后不能立刻说话/录进去”，像“必须等系统放行”

# 必读文件（请实际打开）
1. src/engines/asr-engine.ts
2. src/engines/voice-engine.ts
3. src/engines/echo-guard.ts
4. tools/mic-helper.swift
5. proxy/server.js

# 当前实现概况（供你对齐）
- ASR: 本地 VAD + 云流式识别
- echo-guard: 基于播放参考做相关性匹配 + 自适应抵消
- ASK: HUD 会话 + 重试 + promptEchoFilter
- 已尝试动态“严格非回声”门限，但串音仍可复现
- 用户强约束：
  - 不能退化成“等TTS结束再放ASR”
  - 必须 HUD 一开就可说话（持续录音，不要硬门控）
  - 要多轮实时交互

# 复现信号（最近实测）
- 使用 ask 提问后，识别结果会出现提示词回读，例如：
  - “只回答。”
  - “只回答 a 或 b。”
  - “b 等于没有串音。”
- 用户反馈：仍偶发串音，且感觉录音被“放行机制”阻塞。

# 你要产出的内容（按顺序）
1. 根因定位（必须指向具体代码路径）
   - 请给出“串音为什么还会漏进来”的主因与次因
   - 请给出“为什么用户感觉 HUD 起了但还不能说”的主因与次因
2. 架构修复方案（不是参数微调）
   - 目标: 真正消除/显著降低 TTS→ASR 串音，同时保持低延迟与可插话
   - 给出事件时序图（TTS/ASR/VAD/HUD）与状态机
   - 指出应新增/删减的状态与判定条件
3. 第一版可落地 patch 方案
   - 以最小侵入优先，列出要改的函数与伪代码
   - 如需引入新能力（AEC参考、双通道策略、RNNoise等），说明取舍与回退
4. 验证方案
   - 给出至少 6 条可自动化或半自动的回归用例
   - 覆盖：静音、噪声、用户插话、长提示词、短回答(A/B)、多轮连续 ask
5. 风险与监控
   - 给出线上可观测指标（例如 prompt-echo 命中率、false commit 率、首字延迟）

# 输出格式要求
- 先给 10 行以内结论摘要
- 然后按“根因 -> 修复架构 -> patch计划 -> 验证 -> 风险”分节
- 每个根因都附带文件+函数名定位
- 尽量给出可直接落地到 TypeScript/Swift 的建议
